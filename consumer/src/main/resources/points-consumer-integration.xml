<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-amqp="http://www.springframework.org/schema/integration/amqp"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:int-stream="http://www.springframework.org/schema/integration/stream"
       xsi:schemaLocation="http://www.springframework.org/schema/integration/amqp http://www.springframework.org/schema/integration/amqp/spring-integration-amqp.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/stream http://www.springframework.org/schema/integration/stream/spring-integration-stream.xsd
		http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <int-amqp:inbound-channel-adapter channel="incomingBytes"
                                      queue-names="points"
                                      connection-factory="connectionFactory"/>

    <int:channel id="incomingBytes"/>

    <int:transformer ref="jsonBytesToPointsListTransformer" input-channel="incomingBytes"
                     output-channel="pointsListChannel"/>

    <int:channel id="pointsListChannel"/>

    <int:transformer input-channel="pointsListChannel" output-channel="resultsChannel" ref="pointListTransformer"/>

    <int:channel id="resultsChannel"/>

    <int:object-to-json-transformer input-channel="resultsChannel" output-channel="jsonResultsChannel"/>

    <int:channel id="jsonResultsChannel"/>

    <int-amqp:outbound-channel-adapter id="outboundResultsChannelAdapter"
                                       exchange-name="mandelbrot-exchange"
                                       routing-key="results"
                                       channel="jsonResultsChannel"
                                       />


    <!--<bean name="messageConverter" class="org.springframework.amqp.support.converter.Jackson2JsonMessageConverter"/>-->


    <int-amqp:inbound-channel-adapter channel="incomingResults"
                                      queue-names="results"
                                      connection-factory="connectionFactory"/>

    <int:channel id="incomingResults"/>

    <int:json-to-object-transformer input-channel="incomingResults" output-channel="mandelbrotResultsChannel"/>

    <int:channel id="mandelbrotResultsChannel"/>

    <int:aggregator input-channel="mandelbrotResultsChannel" output-channel="imageDataChannel"/>

    <int:channel id="imageDataChannel"/>

    <int:service-activator id="imageProcessor" input-channel="imageDataChannel" ref="imageService"/>

    <!--<int:logging-channel-adapter channel="imageDataChannel" log-full-message="true" logger-name="bah-results" level="INFO"/>-->







    <rabbit:connection-factory id="connectionFactory" host="docker-host" port="5672"/>

    <rabbit:template id="amqpTemplate" connection-factory="connectionFactory"/>

    <rabbit:admin connection-factory="connectionFactory"/>

    <rabbit:queue name="points" durable="true"/>
    <rabbit:queue name="results" durable="true"/>

    <rabbit:topic-exchange name="mandelbrot-exchange">
        <rabbit:bindings>
            <rabbit:binding queue="points" pattern="points"/>
            <rabbit:binding queue="results" pattern="results"/>
        </rabbit:bindings>
    </rabbit:topic-exchange>

</beans>
